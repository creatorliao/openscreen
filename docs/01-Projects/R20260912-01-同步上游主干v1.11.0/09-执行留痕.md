# 09-执行留痕

**日期**：2026-09-12
**主题**：同步上游主干 v1.11.0

> 四要素：① 改了哪些内容 ② 怎么改的 ③ 有什么作用 ④ 对原工作流程的影响

---

## 改动 1：在途工作备份到 runtime（可回退前提）

| 要素 | 内容 |
|------|------|
| **① 改了什么** | 未改仓库；在 `D:/Users/liaohai1/.pi/agent/runtime/openscreen-backup/` 生成 `dev-creator-tracked-20260912-160622.patch`（9,668 字节） |
| **② 怎么改的** | `git diff > <runtime>/…patch`，仅含已跟踪文件的改动 |
| **③ 有什么作用** | 合并前留一份可回退快照；事后用它**逐行 diff** 证明在途工作无损（V3） |
| **④ 对原工作流程影响** | 无（仓库外，不影响任何行为） |

---

## 改动 2：暂存在途日志改造

| 要素 | 内容 |
|------|------|
| **① 改了哪些内容** | 临时移动 3 个已跟踪文件：`electron/ipc/handlers.ts`、`electron/main.ts`、`electron/stt/index.ts` → `stash@{0}` |
| **② 怎么改的** | `git stash push -m "wip: logging (pre origin/main merge)" <3 个文件>`；**只 stash 已跟踪文件**，`electron/logging/` 与 `docs/01-Projects/R20260908-01-…/` 两个未跟踪目录留在原地 |
| **③ 有什么作用** | 腾出干净工作区供合并；同时避免合并覆盖在途改动 |
| **④ 对原工作流程影响** | 无（含原样还原，见改动 5） |

**为什么不 stash 未跟踪目录**：先核实 `origin/main` 中不存在 `electron/logging` 与 `docs/01-Projects` 这两个路径，因此合并不可能覆盖它们；留在原地反而少一次移动、少一份丢失风险。

---

## 改动 3：合并 `origin/main` 并解 3 处冲突

| 要素 | 内容 |
|------|------|
| **① 改了哪些内容** | 合并提交 **`64b8f243`**。冲突 3 个文件：`package.json`、`package-lock.json`（2 处）、`AGENTS.md`（1 处） |
| **② 怎么改的** | ① `git merge origin/main --no-edit`；② `package.json`/`package-lock.json` 取上游 `1.11.0`；③ `AGENTS.md` 用 `git checkout --ours` 取本地重写版，再**套用上游那 1 行** macOS 内容保护修正；④ `git add` 三个文件后提交，提交信息写明 61 提交 / 冲突解法/ 本地 `build:win:portable` 保留 |
| **③ 有什么作用** | 拿到 v1.11.0 全部修复，含 G3 的转写救命修复（`45c4c8ed`）与 G1 的 AV1 崩溃修复（issue #554）。合并后 `origin/main` 是 `dev-creator` 的祖先，无第二套分叉 |
| **④ 对原工作流程影响** | **有两处行为变化**：<br>1. **录制后自动缩放默认开** —— 新录屏会在时间轴上自动生成光标停留缩放区域；老操作路径不变（仍可手动关），属可选增强<br>2. **HUD 控制行多了「自动缩放」按钮**（位于设备设置按钮之后、光标按钮之前）；系统光标模式下置灰。依赖 HUD 按钮位置的自动化脚本需相应更新 |

**`AGENTS.md` 为什么不整体取上游**：上游对该文件的改动只有 1 行（`git diff 3957fa24 origin/main -- AGENTS.md | grep -c '^@@'` = **1**），而本地是整篇重写（170 → 419 行，含 PARA 落盘约定、Windows Portable 构建规范、Agent 协作规则）。取上游会丢掉这些本地约定。

---

## 改动 4：还原在途日志改造

| 要素 | 内容 |
|------|------|
| **① 改了哪些内容** | `stash@{0}` 弹回，重新落到 `electron/ipc/handlers.ts`（+56）、`electron/main.ts`（+24）、`electron/stt/index.ts`（+28），合计 +108 行 |
| **② 怎么改的** | `git stash pop`；git 报 `Auto-merging electron/ipc/handlers.ts`，**零冲突** |
| **③ 有什么作用** | 在途工作原样归位，合并没有吃掉任何东西 |
| **④ 对原工作流程影响** | 无。但要留意：**零冲突 ≠ 语义正确** —— 上游 `handlers.ts` 同时改了 38+/18−（`RecordingPrefs`、新增 `recordingPrefs.ts`），与本地日志埋点是「加法并存」。已用 patch 逐行比对确认**内容无损**（仅行号偏移）；**语义正确性仍建议人工过一眼**，确认日志埋点还挂在正确的分支上 |

---

## 改动 5：新建主题文档

| 要素 | 内容 |
|------|------|
| **① 改了哪些内容** | 新增 `docs/01-Projects/R20260912-01-同步上游主干v1.11.0/`：`00-README.md`、`01-需求_同步上游主干.md`、`02-分析_上游变更影响.md`、`03-方案_合并与验证.md`、`09-执行留痕.md` |
| **② 怎么改的** | `para-structure cp "同步上游主干v1.11.0"` 建夹（自动得号 `R20260912-01`），再按 AGENTS.md 的编号约定写稿 |
| **③ 有什么作用** | 把「61 个提交改了什么」「为什么这么解冲突」「原生二进制已经落后」三件事落成可追溯文本，其中 §四 的方法论坑与 §六 的探测结论是后续出包的直接依据 |
| **④ 对原工作流程影响** | 无（纯文档） |

---

## 五、本次**没有**做的事（明确记录，避免被读成已完成）

| # | 未做 | 原因 | 何时做 |
|---|------|------|--------|
| N1 | **未重编原生二进制** | 需完整 VS/CMake/Ninja/LLVM/Rust 工具链与代理 | 出包前第一件事 |
| N2 | **未跑 manual e2e** | 原生死链已变（`crates/compositor` +1648 行），不重编就测的是旧代码路径 | 重编后立即 |
| N3 | **未提交在途日志改造** | 见下节 | 由维护者决定 |
| N4 | **未出安装包** | 依赖 N1 | 下一步 |

---

## 六、在途日志改造的处理说明

工作区里 `electron/logging/`（`logger.ts`、`portableLogFile.ts` 各 75 行）+ 3 个文件 +108 行**本次未提交**，理由：

对照该主题自己的方案文档 `docs/01-Projects/R20260908-01-可观测性与日志体系/05-方案_启动自检与全量日志.md`，方案要求交付的文件里有**三项尚未实现**：

| 方案要求 | 实际状态 |
|----------|---------|
| `electron/diagnostics/preflight.ts`（新增：启动自检） | ❌ 文件不存在 |
| `electron/preload.ts`（暴露 `log.forward`） | ❌ 未改动 |
| `electron/stt/whisperServer.ts`（binary-not-found 文案 + 关联 preflight） | ❌ 未改动 |
| `src/` 渲染端 console 转发 | ❌ 未改动 |

而同一篇方案的留痕表自己写着「**本轮＝仅设计**，未改任何代码」。也就是说：**该主题处于设计完成、实现进行中的中间态**，与本次「同步上游」是两件事。

**处置**：不混入本次提交。若维护者要一并入库，建议**单独一条提交**，并先补齐该主题的 `09-执行留痕.md`（该夹目前只有 00–05，缺留痕）。

---

## 七、提交与推送

| 项 | 值 |
|---|---|
| 合并提交 | `64b8f243` Merge remote-tracking branch 'origin/main' into dev-creator |
| 文档提交 | 见 git log（`docs(projects): record the v1.11.0 upstream sync`） |
| 推送目标 | `fork/dev-creator`（`git@github.com:creatorliao/openscreen.git`） |
