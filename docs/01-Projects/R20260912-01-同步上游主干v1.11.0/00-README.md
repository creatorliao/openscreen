# R20260912-01 同步上游主干 v1.11.0

**日期**：2026-09-12
**主线**：把 `getopenscreen/openscreen` 的 `origin/main`（v1.11.0）合并进本仓 `dev-creator`
**目标锚点**：G1（完整路径可用）、G3（字幕/配音）

## 一句话

`dev-creator` 的基线 `3957fa24` 之后，上游已推进 **61 个提交**到 v1.11.0。本次把这批代码合并进来，并**查明本地原生二进制落后于源码**——这决定了后续能不能直接出安装包。

## 文档导航

| 文件 | 内容 |
|------|------|
| `01-需求_同步上游主干.md` | 为什么必须同步、不同步的代价 |
| `02-分析_上游变更影响.md` | 61 个提交干了什么（新功能 / 关键修复 / 风险面），以及冲突成因 |
| `03-方案_合并与验证.md` | 合并策略、冲突解法、验证口径、原生二进制探串方法 |
| `04-执行_重编原生与出包.md` | 环境预检、原生重编、NSIS + portable 出包、产物验证 |
| `09-执行留痕.md` | 四要素留痕（按改动逐条） |

## 结论速览

| 项 | 结果 |
|---|---|
| 合并 | ✅ `64b8f243`，`dev-creator` 相对 `origin/main`：落后 0 / 领先 8 |
| 冲突 | 3 个文件（`package.json`、`package-lock.json`、`AGENTS.md`），全部手工解完 |
| 未提交改动 | ✅ 全部保留，与备份 patch 逐行一致 |
| 静态验证 | ✅ `tsc` ×2、`npm run test` 2761 passed、`i18n:check` |
| 原生二进制 | ✅ **已重编**，AV1 标记串由 ABSENT 翻为 HIT |
| 出包 | ✅ NSIS 安装包 + portable 目录，包内负载哈希 == 本地新构建 |
| **应用实跑** | ⛔ **未做** —— 原生改动必须走 manual e2e，这是当前最大缺口 |

## 产物

| 产物 | 路径 |
|------|------|
| NSIS 安装包 | `release/1.11.0/Openscreen.Setup.1.11.0.exe`（≈214.5 MiB） |
| Portable 目录 | `release/openscreen-portable-1.11.0/`（`Openscreen.exe` 在最外层） |
